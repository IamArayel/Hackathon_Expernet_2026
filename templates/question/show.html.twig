{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ question.intitule }}</td>
            </tr>
            <tr>
                <th>Sous_texte</th>
                <td>{{ question.sousTexte }}</td>
            </tr>
            <tr>
                <th>ReponseUtilisateur</th>
                <td>{{ question.reponseUtilisateur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_question_index') }}">back to list</a>

    <a href="{{ path('app_question_edit', {'id': question.id}) }}">edit</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
